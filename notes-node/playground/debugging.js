var person = {
  name: 'Daniel'
};

person.age = 27;
debugger;
person.name = 'Mike';

console.log(person);

// node inspect filename.js
// list(number of rows to list)
// n -> next
// c -> continue
// repl mode
